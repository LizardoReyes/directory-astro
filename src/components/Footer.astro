---
import { getTranslations } from '../i18n';
import { getSiteInfo } from "../libs/csv";

const { name, language } = getSiteInfo();
const t = getTranslations(language);
const year = new Date().getFullYear();
---

<footer class="bg-[var(--color-header-bg)] text-[var(--color-header-text)] text-center py-4 border-t border-border transition-colors duration-300">
  © {year} {name}. {t.MESSAGE_FOOTER}
</footer>
