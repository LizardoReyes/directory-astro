---
import { createSlug, getDateTimeString } from "../../libs/global.ts";
import type { PostType } from "../../types/PostType";
import OptimizedImage from "../images/OptimizedImage.astro";
import {createPostUrl} from "../../libs/urls";
import {getItemById, getSiteInfo} from "../../libs/csv";
import {categoriesFilePath} from "../../libs/constants";
import type {CategoryType} from "../../types/CategoryType";
import {getTranslations} from "../../i18n";

type PostProps = {
  post: PostType;
};

const { language } = getSiteInfo();
const t = getTranslations(language);
const { post } = Astro.props as PostProps;
const category = getItemById<CategoryType>(categoriesFilePath, post.category_id);
---

<!-- Artículo -->
<div class="bg-bg text-text rounded-lg shadow-sm p-5 flex flex-col border border-border transition-colors duration-300">
  <a href={createPostUrl(post, category)}>
    <OptimizedImage
        src={post.image}
        alt={post.title}
        class="rounded-lg mb-4 w-full h-48 object-cover"
    />
  </a>

  <h2 class="text-lg font-bold text-center text-primary mb-2">
    <a href={createPostUrl(post, category)} class="hover:underline">
      {post.title}
    </a>
  </h2>

  <p class="text-xs text-muted text-center mb-2">
    📅 {getDateTimeString(post.publishedAt)} — ✍️ Juan Pérez
  </p>

  <p class="text-sm text-center text-text mb-4 flex-grow" set:html={post.content}></p>

  <a
      href={createPostUrl(post, category)}
      class="inline-block bg-primary hover:bg-primary-hover text-center text-text text-sm font-semibold px-6 py-2.5 rounded-lg shadow-md hover:shadow-lg ring-1 ring-border transition duration-150 ease-in-out"
  >
    {t.MESSAGE_CLICK_POST}
  </a>
</div>
