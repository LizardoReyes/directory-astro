---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Contáctanos" description="Página de contacto del Restaurante Afrodita">
  <!-- Main -->
  <main class="container mx-auto px-4 py-10">
    <section class="bg-white shadow-md rounded-lg p-8 max-w-2xl mx-auto">
      <h1 class="text-3xl font-bold text-gray-800 mb-6">Contáctanos</h1>
      <p class="text-gray-600 mb-8">
        Si tienes preguntas, sugerencias o deseas colaborar con nosotros, no
        dudes en enviarnos un mensaje. Te responderemos lo antes posible.
      </p>

      <form action="#" method="POST" class="space-y-6">
        <!-- Nombre -->
        <div>
          <label for="name" class="block text-sm font-medium text-gray-700 mb-1"
            >Nombre</label
          >
          <input
            type="text"
            id="name"
            name="name"
            required
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-blue-200"
          />
        </div>

        <!-- Correo -->
        <div>
          <label
            for="email"
            class="block text-sm font-medium text-gray-700 mb-1"
            >Correo electrónico</label
          >
          <input
            type="email"
            id="email"
            name="email"
            required
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-blue-200"
          />
        </div>

        <!-- Mensaje -->
        <div>
          <label
            for="message"
            class="block text-sm font-medium text-gray-700 mb-1">Mensaje</label
          >
          <textarea
            id="message"
            name="message"
            rows="5"
            required
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-blue-200"
          ></textarea>
        </div>

        <!-- Botón -->
        <div class="flex justify-center">
          <button
            type="submit"
            class="bg-blue-600 text-white px-20 py-2 mx-auto rounded hover:bg-blue-700 transition"
          >
            Enviar mensaje
          </button>
        </div>
      </form>

      <!-- Información adicional -->
      <div class="mt-10 text-sm text-gray-600">
        <p><strong>Email:</strong> contacto@restauranteafrodita.com</p>
        <p>
          <strong>Horario de atención:</strong> Lunes a Viernes, 9:00 - 18:00
        </p>
      </div>

      <!-- Informacion de envio correcto -->
      <div class="mt-10 text-sm text-green-600">
        <p id="success-message" class="text-center">
          ¡Mensaje enviado con éxito!
        </p>
      </div>
      <div class="mt-10 text-sm text-red-600">
        <p id="error-message" class="text-center">
          Error al enviar el mensaje. Por favor, inténtalo de nuevo.
        </p>
      </div>
    </section>
  </main>
</Layout>
